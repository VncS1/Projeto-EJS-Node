<!DOCTYPE html>

<html> 

    <!-- Incluindo o arquivo head no index.ejs -->
    <%- include('../partials/head'); %>

    <body class="container">
        <%- include('../partials/header'); %>
        <main>
            <div class="jumbotron">
                <h1>Node com EJS</h1>
                <p>Vamos aprender a trabalhar com arquivos EJS</p>
                <p><%= subtitle %></p>
            </div>

            <ul>
                <!-- Exibindo cada elemento que foi recebido pelo server em uma função anonima -->
                <%  qualitys.forEach(function(quality){ %>
                    <li>
                        <strong><%=quality.title %></strong> <%=quality.message%>
                    </li>
                <%  }); %>
                
            </ul>

        </main>
        <%- include('../partials/footer'); %>
    </body>
</html>